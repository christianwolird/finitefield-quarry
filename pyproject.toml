[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "finitefield-quarry"
version = "0.1.0"
description = "Search for square patterns and perfect bricks over finite fields."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "sympy",
    "galois",
]

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["ffquarry"]
